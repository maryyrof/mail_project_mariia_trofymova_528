<?php
echo "<h2>Завдання 3.1</h2>";

// 1. 201/3 + 1582
echo "201/3 + 1582 = " . (201/3 + 1582) . "<br>";

// 2. Вираз з змінною
$x = 14;
$result = 14/209 + $x*(29 - 132 + 14/3);
echo "Результат виразу = $result<br>";

// 3. Сума цифр двозначного числа
$n = 57;
$sum_digits = (int)($n/10) + ($n%10);
echo "Сума цифр $n = $sum_digits<br>";

// 4. Округлення дробового числа
$n = 12.7;
echo "Округлення $n = " . round($n) . "<br>";

// 5. Сума цифр тризначного числа
$n = 345;
$sum_digits = (int)($n/100) + (int)(($n%100)/10) + ($n%10);
echo "Сума цифр $n = $sum_digits<br>";

// 6. Ділення з залишком
$q = 21; $w = 8;
$div = (int)($q/$w);
$rem = $q % $w;
echo "$q / $w = $div і $rem у залишку<br>";

echo "<h2>Завдання 3.2</h2>";

// Площа та периметр прямокутного трикутника
$a = 3; $b = 4;
$area = 0.5*$a*$b;
$perimeter = $a + $b + sqrt($a*$a + $b*$b);
echo "Площа = $area, Периметр = $perimeter<br>";

// Кількість цифр у натуральному числі
$n = 12345;
echo "Цифр у числі $n = " . strlen((string)$n) . "<br>";

// Функція сигнум
$n = -3.7;
$sign = ($n>0) ? 1 : (($n<0)? -1 : 0);
echo "Signum($n) = $sign<br>";

echo "<h2>Завдання 3.3</h2>";

// Речове число [-3;3]
$rand_float = -3 + mt_rand()/mt_getrandmax()*6;
echo "Випадкове число [-3;3] = $rand_float<br>";

// Ціле число [-n;n]
$n = 10;
$rand_int = mt_rand(-$n,$n);
echo "Випадкове ціле [-$n;$n] = $rand_int<br>";

// Ціле число [a;b]
$a = 5; $b = 15;
$rand_int = mt_rand($a,$b);
echo "Випадкове ціле [$a;$b] = $rand_int<br>";

echo "<h2>Завдання 3.4</h2>";

// Парність числа
$n = 7;
echo "$n - " . (($n%2==0)?"парне":"непарне") . "<br>";

// Найближче до 10
$m = 8.5; $n = 11.45;
$closest = (abs($m-10) < abs($n-10)) ? $m : $n;
echo "Найближче до 10 = $closest<br>";

// Квадратне рівняння
$a = 1; $b = -3; $c = 2;
$d = $b*$b - 4*$a*$c;
if ($d<0) {
    echo "Коренів немає<br>";
} elseif ($d==0) {
    $x = -$b/(2*$a);
    echo "Корінь = $x<br>";
} else {
    $x1 = (-$b + sqrt($d))/(2*$a);
    $x2 = (-$b - sqrt($d))/(2*$a);
    echo "Корені: $x1, $x2<br>";
}

echo "<h2>Завдання 3.5</h2>";

// Перевірка числа в інтервалі (25;100)
$n = mt_rand(5,155);
echo "Число $n " . (($n>25 && $n<100)?"міститься":"не міститься") . " в інтервалі (25;100)<br>";

// Випадкове тризначне число та найбільша цифра
$n = mt_rand(100,999);
$digits = str_split($n);
$max_digit = max($digits);
echo "Число $n, найбільша цифра $max_digit<br>";

// Сортування трьох чисел
$a = 3; $b = 9; $c = -1;
$arr = array($a,$b,$c);
sort($arr);
echo "Зростаюча послідовність: " . implode(", ", $arr) . "<br>";

// Переведення секунд у години
$n = mt_rand(0,28800);
echo "$n секунд<br>";
$hours = (int)($n/3600);
if ($hours>=1) {
    echo "Залишилось $hours " . ($hours==1?"година":"години") . "<br>";
} else {
    echo "Залишилось менше години<br>";
}

echo "<h2>Завдання 3.6</h2>";
if (isset($argv[1])) {
    if (ctype_digit($argv[1])) {
        $n = (int)$argv[1];
        echo "$n - " . (($n%2==0)?"парне":"непарне") . "\n";
    } else echo "Помилка: введено не ціле число\n";
}
?>